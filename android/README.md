# Orbit Android 1.12.0

Android companion app for Orbit. It uses the same Render-hosted Orbit account site and therefore shares the same User / Helper / Admin roles and title system.

Build with Android Studio or Gradle:
- `gradle assembleRelease` → APK
- `gradle bundleRelease` → AAB
